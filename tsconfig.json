{
  "compilerOptions": {
    "outDir": "./dist", //  打包输出的目录,不配置也没事,因为在webpack已经配置了
    "module": "es6", //  打包的是es6代码
    "target": "es5", //  打包出来的内容是 es5,在大部分浏览器红都能使用
    "allowJs": true //  允许引入js
  },
  "include": ["src"] //  对src的代码下有效
}
